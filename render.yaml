services:
  - type: web
    name: event-portal-flask
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      # Set DATABASE_URL in the dashboard if you add Postgres
